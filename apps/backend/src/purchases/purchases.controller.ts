import { Body, Controller, Get, Post, Req, UseGuards } from '@nestjs/common'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { AuthRequest } from '../common/auth-request'; import { CreatePurchaseDto } from './dto/create-purchase.dto'; import { PurchasesService } from './purchases.service'; @UseGuards(JwtAuthGuard) @Controller('purchases') export class PurchasesController { constructor(private s:PurchasesService){} @Get() list(@Req() r:AuthRequest){return this.s.list(r.user.tenantId)} @Post() create(@Req() r:AuthRequest,@Body() d:CreatePurchaseDto){return this.s.create(r.user.tenantId,d)} }
