import { Controller, Get, Param, Req, UseGuards } from '@nestjs/common'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { AuthRequest } from '../common/auth-request'; import { CouponsService } from './coupons.service'; @UseGuards(JwtAuthGuard) @Controller('coupons') export class CouponsController { constructor(private s:CouponsService){} @Get() list(@Req() r:AuthRequest){return this.s.list(r.user.tenantId)} @Get('by-consumer/:consumerId') byConsumer(@Req()r:AuthRequest,@Param('consumerId') c:string){return this.s.byConsumer(r.user.tenantId,c)} @Get('by-campaign/:campaignId') byCampaign(@Req()r:AuthRequest,@Param('campaignId') c:string){return this.s.byCampaign(r.user.tenantId,c)} }
