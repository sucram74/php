import { Injectable } from '@nestjs/common'; import { PrismaService } from '../prisma.service'; @Injectable() export class CouponsService { constructor(private prisma:PrismaService){} list(tenantId:string){return this.prisma.coupon.findMany({where:{tenantId},include:{campaign:true,consumer:true},orderBy:{generatedAt:'desc'}})} byConsumer(tenantId:string,consumerId:string){return this.prisma.coupon.findMany({where:{tenantId,consumerId},orderBy:{generatedAt:'desc'}})} byCampaign(tenantId:string,campaignId:string){return this.prisma.coupon.findMany({where:{tenantId,campaignId},orderBy:{generatedAt:'desc'}})} }
