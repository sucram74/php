import { Injectable, NotFoundException } from '@nestjs/common'; import { PrismaService } from '../prisma.service'; import { CreateCampaignDto } from './dto/create-campaign.dto'; import { UpdateCampaignDto } from './dto/update-campaign.dto';
@Injectable() export class CampaignsService { constructor(private prisma:PrismaService){} list(tenantId:string){return this.prisma.campaign.findMany({where:{tenantId},orderBy:{createdAt:'desc'}})} async get(tenantId:string,id:string){const c=await this.prisma.campaign.findFirst({where:{tenantId,id}}); if(!c) throw new NotFoundException('Campanha não encontrada'); return c;} create(tenantId:string,d:CreateCampaignDto){return this.prisma.campaign.create({data:{tenantId,...d,startDate:new Date(d.startDate),endDate:new Date(d.endDate)}})} update(tenantId:string,id:string,d:UpdateCampaignDto){return this.get(tenantId,id).then(()=>this.prisma.campaign.update({where:{id},data:{...d,startDate:d.startDate?new Date(d.startDate):undefined,endDate:d.endDate?new Date(d.endDate):undefined}}))} async toggle(tenantId:string,id:string){const c=await this.get(tenantId,id); return this.prisma.campaign.update({where:{id},data:{active:!c.active}})} }
