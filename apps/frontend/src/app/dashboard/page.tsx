'use client'; import { useEffect, useState } from 'react'; import { api } from '@/lib/api'; export default function(){const [d,setD]=useState<any>({}); useEffect(()=>{void Promise.all([api('/campaigns'),api('/consumers'),api('/purchases'),api('/coupons')]).then(([c,co,p,cu])=>setD({c:c.length,co:co.length,p:p.length,cu:cu.length}));},[]); return <div className='grid grid-cols-4 gap-4'>{[['Campanhas',d.c],['Consumidores',d.co],['Compras',d.p],['Cupons',d.cu]].map(([t,v])=><div key={String(t)} className='border p-4'><p>{String(t)}</p><strong>{v??0}</strong></div>)}</div>}
