'use client'; import { useEffect, useState } from 'react'; import { api } from '@/lib/api'; export default function(){const [items,setItems]=useState<any[]>([]); const [cpf,setCpf]=useState(''); const [msg,setMsg]=useState(''); const [form,setForm]=useState({cpf:'',name:'',phone:''}); const labels:{[k:string]:string}={cpf:'CPF',name:'Nome',phone:'Telefone'}; const load=()=>api('/consumers').then(setItems); useEffect(()=>{void load();},[]); return <div><h1>Consumidores</h1><div className='space-x-2'><input className='border' placeholder='Digite o CPF' value={cpf} onChange={e=>setCpf(e.target.value)}/><button onClick={()=>api(`/consumers/by-cpf/${cpf}`).then(r=>setItems(r?[r]:[]))}>Buscar CPF</button><button onClick={load}>Limpar</button></div><div className='space-x-2 mt-3'>{Object.keys(form).map(k=><input key={k} className='border' placeholder={labels[k]} value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/>)}<button onClick={async()=>{await api('/consumers',{method:'POST',body:JSON.stringify(form)});setForm({cpf:'',name:'',phone:''});setMsg('Consumidor cadastrado com sucesso.');load();}}>Cadastrar</button></div>{msg&&<p>{msg}</p>}<table><thead><tr><th>CPF</th><th>Nome</th><th>Telefone</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.cpf}</td><td>{i.name}</td><td>{i.phone}</td></tr>)}</tbody></table></div>}
