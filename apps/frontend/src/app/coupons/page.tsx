'use client'; import { useEffect, useState } from 'react'; import { api } from '@/lib/api'; export default function(){const [items,setItems]=useState<any[]>([]); const [campaignId,setCampaignId]=useState(''); useEffect(()=>{void api('/coupons').then(setItems);},[]); return <div><h1>Cupons</h1><input className='border' placeholder='campaignId' value={campaignId} onChange={e=>setCampaignId(e.target.value)}/><button onClick={()=>api(campaignId?`/coupons/by-campaign/${campaignId}`:'/coupons').then(setItems)}>Filtrar</button>{items.map(i=><div key={i.id}>{i.couponCode}</div>)}</div>}
