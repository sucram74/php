'use client';
import Link from 'next/link'; import { useEffect, useMemo } from 'react'; import { usePathname, useRouter } from 'next/navigation'; import { APP_VERSION } from '@/lib/app-config'; import { Button } from '@/components/ui/button'; import { Tutorial } from './tutorial';
const menuItems=[['dashboard','Painel da Loja'],['admin','Painel Marcus'],['consumers','Consumidores'],['campaigns','Campanhas'],['purchases','Compras'],['coupons','Cupons'],['draws','Sorteios'],['credits','Créditos']];
function decodeToken(token:string|null){if(!token)return{}; try{return JSON.parse(atob(token.split('.')[1]));}catch{return{}}}
export function AdminShell({children}:{children:React.ReactNode}){const router=useRouter(); const pathname=usePathname(); useEffect(()=>{if(pathname!=='/login'&&!localStorage.getItem('token')) router.push('/login');},[pathname,router]); const footerData=useMemo(()=>{const t:any=decodeToken(typeof window!=='undefined'?localStorage.getItem('token'):null); return {store:t.tenantName||t.storeName||'Loja Demo', user:t.email||'admin@demo.com'};},[pathname]); if(pathname==='/login')return<>{children}</>; return <div className='flex h-screen overflow-hidden'><Tutorial/><aside className='w-64 h-screen shrink-0 bg-slate-900 p-4 text-white flex flex-col'><h2 className='mb-6 text-xl font-bold'>Promo SaaS</h2><nav className='space-y-1'>{menuItems.map(([k,l])=><Link key={String(k)} className='block rounded-md px-3 py-2 hover:bg-white/20' href={`/${k}`}>{String(l)}</Link>)}<Button variant='secondary' className='w-full justify-start' onClick={()=>{localStorage.removeItem('tutorial_done')}}>Ver tutorial novamente</Button><Button variant='secondary' className='w-full justify-start' onClick={()=>{localStorage.removeItem('token');router.push('/login')}}>Sair</Button></nav><div className='mt-auto border-t border-white/20 pt-4 text-sm'><p>Loja: {footerData.store}</p><p>Usuário: {footerData.user}</p><p>Versão: {APP_VERSION}</p></div></aside><main className='flex-1 h-screen overflow-y-auto p-6'>{children}</main></div>}
