'use client';
import Link from 'next/link'; import { useEffect, useMemo, useState } from 'react'; import { usePathname, useRouter } from 'next/navigation'; import { APP_VERSION } from '@/lib/app-config'; import { Button } from '@/components/ui/button'; import { Tutorial } from './tutorial'; import { Toast } from './ui/toast'; import { HelpBot } from './help-bot';
const lojaMenu=[['dashboard','Painel da Loja'],['consumers','Consumidores'],['campaigns','Campanhas'],['purchases','Compras'],['coupons','Cupons'],['draws','Sorteios'],['credits','Créditos']];
const adminMenu=[['admin','Painel Marcus'],['campaigns','Campanhas'],['consumers','Consumidores'],['coupons','Cupons'],['draws','Sorteios'],['credits','Créditos / Pacotes']];
function decodeToken(token:string|null){if(!token)return{}; try{return JSON.parse(atob(token.split('.')[1]));}catch{return{}}}
export function AdminShell({children}:{children:React.ReactNode}){const router=useRouter(); const pathname=usePathname(); const [allowAdmin,setAllowAdmin]=useState(false); const [toast,setToast]=useState(''); useEffect(()=>{if(pathname!=='/login'&&!localStorage.getItem('token')) router.push('/login');},[pathname,router]); const footerData=useMemo(()=>{const t:any=decodeToken(typeof window!=='undefined'?localStorage.getItem('token'):null); return {store:t.tenantName||t.storeName||'Loja Demo', user:t.email||'admin@demo.com', role:(t.role||'store').toLowerCase()};},[pathname]); useEffect(()=>{if(footerData.role!=='admin'&&pathname.startsWith('/admin'))router.push('/dashboard'); if(footerData.role==='admin'&&['/dashboard','/purchases'].includes(pathname))router.push('/admin');},[footerData.role,pathname,router]); useEffect(()=>{let x=0;let y=0; const onKey=(ev:KeyboardEvent)=>{if(footerData.role==='admin')return; if(ev.key.toLowerCase()==='x'){x+=1;if(x>=5){setAllowAdmin(true);setToast('Menu administrativo liberado temporariamente.');x=0;}}else if(ev.key.toLowerCase()==='y'){y+=1;if(y>=5){setAllowAdmin(false);setToast('Menu administrativo ocultado.');y=0;}}}; window.addEventListener('keydown',onKey); return()=>window.removeEventListener('keydown',onKey);},[footerData.role]); if(pathname==='/login')return<>{children}</>; const menus = footerData.role==='admin' ? adminMenu : [...lojaMenu, ...(allowAdmin?adminMenu:[])]; return <div className='flex h-screen overflow-hidden'><Tutorial/><HelpBot/><Toast message={toast} onClose={()=>setToast('')}/><aside className='w-64 h-screen shrink-0 bg-slate-900 p-4 text-white flex flex-col'><h2 className='mb-6 text-xl font-bold'>Promo SaaS</h2><nav className='space-y-1'>{menus.map(([k,l])=><Link key={String(k)} className='block rounded-md px-3 py-2 hover:bg-white/20' href={`/${k}`}>{String(l)}</Link>)}<Button variant='secondary' className='w-full justify-start' onClick={()=>{localStorage.removeItem('tutorial_done')}}>Ver tutorial novamente</Button><Button variant='secondary' className='w-full justify-start' onClick={()=>{localStorage.removeItem('token');router.push('/login')}}>Sair</Button></nav><div className='mt-auto border-t border-white/20 pt-4 text-sm'><p>Loja: {footerData.store}</p><p>Usuário: {footerData.user}</p><p>Versão: {APP_VERSION}</p></div></aside><main className='flex-1 h-screen overflow-y-auto p-6'>{children}</main></div>}
